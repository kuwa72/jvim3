# JVim 3 の使い方

[English](USAGE.md) | **日本語**

動かすまでの手順と、素の vi ではない部分 — 文字コード、IME 制御、Win32 GUI —
のまとめです。全コマンド・全オプションは Vim 3.0 のリファレンス
[doc/reference.doc](doc/reference.doc) と、JVim の説明書
[doc.j/readme.doc](doc.j/readme.doc) にあります。ここはそこまで読まずに動かす
ための近道です。

- [起動する](#起動する)
- [Windows での最初の設定](#windows-での最初の設定)
- [Unix での最初の設定](#unix-での最初の設定)
- [出発点になる `_vimrc`](#出発点になる-_vimrc)
- [文字コード](#文字コード)
- [日本語入力](#日本語入力)
- [表示とフォント](#表示とフォント)
- [vi に対して増えているもの](#vi-に対して増えているもの)
- [JVim が足したもの](#jvim-が足したもの)
- [削除した機能](#削除した機能)
- [設定はどこに保存されるか](#設定はどこに保存されるか)
- [トラブルシューティング](#トラブルシューティング)

## 起動する

| | |
| --- | --- |
| `jvim32w.exe` | Windows、GUI 版。自前のウィンドウを持ち、コンソールは使いません。 |
| `jvim32.exe` | Windows、コンソールから起動する版。`-nw` を付けない限り GUI ウィンドウを開きます。 |
| `jvim3` | Unix。起動した端末で動きます。 |

64 ビットのパッケージではそれぞれ `jvim64w.exe`、`jvim64.exe` です。

起動オプション。印のないものは vi と Vim 3.0 のものです。

```
jvim3 [options] [file ..]
jvim3 [options] -t tag
jvim3 [options] -e [errorfile]
```

| | |
| --- | --- |
| `-v`, `-R` | 読み取り専用 (view)。 |
| `-n` | スワップファイルを作らず、メモリだけで動く。 |
| `-b` | バイナリモード。 |
| `-r` | リカバリモード。クラッシュ後にスワップファイルから復元する。 |
| `-T terminal` | `$TERM` の代わりに使う端末種別。 |
| `-o[N]` | N 個のウィンドウを開く。既定は指定したファイルごとに 1 つ。 |
| `+`, `+lnum` | ファイル末尾、または `lnum` 行目から始める。 |
| `-c command` | 最初に実行する ex コマンド。 |
| `-s scriptin` | スクリプトファイルからコマンドを読む。 |
| `-w scriptout` | 打ったキーをすべてスクリプトファイルに書き出す。 |
| `-k code` | JVim: ファイルの文字コード (`jcode`)。`E J S U T`。小文字にすると自動判定しない。 |
| `-K mask` | JVim: `jmask` を 4 文字で指定。[文字コード](#文字コード)参照。 |
| `-nw` | Windows: ウィンドウを開かずコンソールで動く。 |
| `-no` | Windows: 単一インスタンス (One Window)。すでに動いている JVim があればそこでファイルを開く。メニューの `One Window` と同じ。関連付けからの起動 (`jvim32w.exe -no "%1"`) に向きます。 |
| `-n0` … `-n3` | Windows: レジストリに保存された 4 つの GUI プロファイルのどれを使うか。 |
| `-I section` | Windows: `vim32.ini` のどのセクションを読むか。 |

環境変数:

| | |
| --- | --- |
| `VIM` | ヘルプファイル、システム全体の `vimrc`、シンタックスルールを探す場所。未設定ならエディタが補います (Windows は実行ファイルのあるディレクトリ、Unix は `$PREFIX/lib/jvim3`)。 |
| `HOME` | `.vimrc` / `_vimrc` を置く場所。Windows で未設定なら `%HOMEDRIVE%%HOMEPATH%`、それも無ければ実行ファイルのディレクトリ。 |
| `VIMINIT`, `EXINIT` | rc ファイルの代わりに起動時に実行する ex コマンド。 |
| `TERM` | Unix: 使う端末定義。 |
| `LANG`, `LC_CTYPE` | Unix: `jmask` の既定値を決めます。[文字コード](#文字コード)参照。 |
| `TMP` | `backupdir` / `directory` が指していればバックアップとスワップの置き場所。 |

## Windows での最初の設定

zip を好きな場所に展開します。`%VIM%` の設定は不要です (実行ファイルのディレク
トリが使われます)。そのうえで:

**1. `:help` を確認する (英語版にしたいなら差し替える)。** パッケージのヘルプ
ファイルは `vim.hlp` で、これはエディタが探す名前 (`$VIM\vim.hlp`) そのものなので、
展開したままで `:help` が使えます。中身は JVim の日本語ヘルプです。英語版が欲しけ
れば、このリポジトリの [doc/vim.hlp](doc/vim.hlp) を上書きコピーするか、両方置いて
`helpfile` で切り替えてください。

```vim
set helpfile=$VIM\vim-en.hlp
```

`v3.0-j2.1b-utf8.4` までのパッケージは `jvim3.hlp` という名前で同梱していました。
これはどこからも探されない名前なので、リネームするか `helpfile` で指定してください。

**2. rc ファイルを読まれる場所に置く。** 起動時に読まれる順序は次のとおりです。

1. `%VIM%\vimrc` — アンダースコアが付かない点に注意
2. 続いて、次の**最初に見つかったもの 1 つだけ**: `%VIMINIT%`、`%HOME%\_vimrc`、`%EXINIT%`、`%HOME%\_exrc`
3. `exrc` オプションが設定されている場合にかぎり、カレントディレクトリの `_vimrc`、次に `_exrc`

exe の隣に置く `%VIM%\vimrc` が一番簡単で、展開したディレクトリだけで完結します。
パッケージの `_jvimrc.sample` は土田さん自身の 2002 年当時の設定で、読む価値があり
ます。もう通らない 2 つの設定 (`decode` と `keywordprg=vshelp.exe`) はコメントアウト
してありますが、`tags` の行は手元にない Visual C++ 6 を指しています。もっと短い今の
設定例は[下](#出発点になる-_vimrc)にあります。

**3. フォントを設定する。** メニューの `Global > Font` から。等幅フォントしか出て
きませんが、これは仕様です。文字グリッド上に描画しているので、プロポーショナル
フォントでは成立しません。日本語が入っているフォントなら何でも構いません。MS
ゴシックはどの Windows にもあり、Myrica や BIZ UDゴシックはより綺麗です。一度
選べば、画面の拡大率ごとに記憶されます。

**4. 以上です。** フォント、ウィンドウサイズ、色、メニューとスクロールバーの有無は
終了時にレジストリ (`HKEY_CURRENT_USER\Software\Vim`) に保存されます。

## Unix での最初の設定

```sh
./scripts/build-unix.sh          # src/jvim3
sudo install -m 755 src/jvim3     /usr/local/bin/
sudo install -m 644 doc.j/vim.hlp /usr/local/lib/jvim3.hlp    # 英語版は doc/vim.hlp
```

`:help` が探すのは `/usr/local/lib/jvim3.hlp` です (`set helpfile=...` で変更可)。
`/usr/local/etc/jvim3rc` があれば何よりも先に読まれるので、システム共通の設定に
向きます。その後は Windows と同じで、`$VIMINIT`、`$HOME/.vimrc`、`$EXINIT`、
`$HOME/.exrc` のうち最初に見つかった 1 つ、そして `exrc` が設定されていれば
`./.vimrc` か `./.exrc` です。

文字コードの既定値はロケールから決まるので、普通の UTF-8 環境なら文字コードの
設定は何もいりません。

```sh
LANG=ja_JP.UTF-8   # jmask=TTTT — キー入力・表示・パイプ・新規ファイルすべて UTF-8
LANG=ja_JP.eucJP   # jmask=EEEE
LANG=C             # jmask=EEET — 表示は EUC、新規ファイルは UTF-8
```

端末で日本語が空白になったり幅がずれたりする場合は、エディタではなく端末のフォント
の問題です。`echo $TERM` が実際の端末と合っているかも確認してください。

## 出発点になる `_vimrc`

足すより削って使ってください。ここにあるものはすべてこの版に存在します。

```vim
" ---- 編集
set autoindent
set backspace=2         " 自動インデント・改行・挿入開始位置を越えて BS が効く
set tabstop=4
set shiftwidth=4
set noexpandtab
set textwidth=0
set whichwrap=11        " BS、スペース、カーソルキーが行頭行末をまたぐ
set wildchar=9          " : 行で TAB がファイル名を補完する
set history=100
set showcmd
set showmatch
set ruler
set laststatus=1
set nobackup            " または set backup / set backupdir=>$TMP
set directory=>$TMP     " スワップは必ずここへ (Unix なら >/tmp)

" ---- 日本語
set nojkanaconv         " 半角カナをそのままにする (現在の既定値)
set fepctrl             " 挿入モードを抜けるとき IME を切る
set fepkey=\\           " CTRL-\ で IME をトグル
set jinsertmode=a       " 挿入モードは ASCII で始まる
" set jmask=TTTT        " ロケールが当てにならないときだけ

" ---- 色付け (GUI でも端末でも)
set fexrc               " 開いたファイルの種別に応じたルールを読む
set syntax
set syntype=cfp
source $VIM/syntax/filetype.jvsyn

" ---- Windows GUI
set crmark              " 行末を表示する
```

複数の言語を扱うなら `fexrc` は覚えておくと便利です。これを設定すると `main.c` を
開いたときに `.vimrc.c` があればそれを読むので、タブ幅をファイル種別に追随させら
れます。1 つの `.vimrc` の中に拡張子別のブロックを書く方法も含め、詳しくは
`doc.j/readme.doc` §6.13 にあります。

## 文字コード

**エディタの中はすべて UTF-8 です。** 変換はテキストが入ってくるとき・出ていくとき
に行われ、その境界が何を話すかを決めるのが `jmask` です。4 文字で、**キー入力・
画面表示・システム・ファイル**の順です。

| 位置 | 意味 | |
| --- | --- | --- |
| 1 | キー入力 | キーボードから来るもの。Win32 GUI ではウィンドウが Unicode なので、この文字が何であれ常に UTF-8 です。この位置が意味を持つのはコンソール版です。 |
| 2 | 画面表示 | 端末に書き出す符号。Unicode で直接描画する GUI では無視されます。 |
| 3 | システム | パイプ。`:!` で起動したコマンドが読み書きする符号。Windows では `S` (CP932) のままにします。Windows のコンソールプログラムが今も話すのはこれです。 |
| 4 | ファイル | **新規**ファイルを書き出すときの符号。 |

各文字は `E` EUC-JP、`J` ISO-2022-JP、`S` Shift-JIS、`T` UTF-8 のいずれかです
(`U` = UCS-2 は `jcode` 専用です。キーボードやパイプの符号としては意味を持ちません)。

```
Windows の既定   SSST     コンソールとパイプは CP932、新規ファイルは UTF-8
Unix の既定      EEET     LANG が別の指定をしていなければ (上記参照)
すべて UTF-8     TTTT
```

設定は `:set jm=TTTT`、確認は `:set jm?`、起動時なら `-K TTTT` です。3 文字の
`jmask` も従来どおり使えて、「新規ファイルはシステムコードで書く」という意味に
なります。4 文字目が存在しなかった頃と同じ挙動です。

**`jcode` はウィンドウごと**の設定で、そのウィンドウで開いているファイルの符号です
(`E J S U T`)。**大文字**にすると「読み込み時に自動判定する」、小文字だと「しない」
という意味になります。

```vim
:set jc?          " このバッファは何か
:set jc=t         " これ以降 UTF-8 で書き出す
```

```sh
jvim3 -k s file.txt      # 自動判定せず Shift-JIS として読み書きする
jvim3 -k S file.txt      # 自動判定が別の答えを出さなければ Shift-JIS
```

ファイルの変換は、開いて (自動判定が正しく効きます)、`:set jc=t`、`:w` です。

**自動判定**はバッファ全体を見ます。UTF-8 として解釈でき、マルチバイト文字が 1 つ
以上あれば UTF-8 と判断します。Shift-JIS や EUC の文章がこの条件を満たすことは
ありません。後続バイトが UTF-8 の継続バイトの範囲から外れるからです。BMP 外の
文字も他と同様に判断材料になります。この 2 点はどちらも以前は正しくなく、それが
日本語 UTF-8 が Shift-JIS と誤判定されていた理由であり、絵文字 1 個でファイル全体
の判定がひっくり返っていた理由です。

**往復できるもの、できないもの:**

| | |
| --- | --- |
| UTF-8、UCS-2 | バイト単位で往復します。BOM、CP932 外の文字、BMP 外の文字を含めて。 |
| EUC-JP、Shift-JIS、ISO-2022-JP | その符号にある文字だけ。ほかは `?` になります。仕様です。 |
| UCS-2 の読み込み | いまも CP932 を経由するため、CP932 外の文字は**読み込み時に**失われます。先に UTF-8 に変換してください。書き出しは直接変換で無損失です。 |
| 結合文字 | 保持され保存もされますが、幅 0 で描画されるため見えません。 |
| 半角カナ | そのままにします。読み込み時に全角へ書き換えていた `jkanaconv` は既定で off になりました。`:set jkc` で戻せます。 |

`jbigendian` は UCS-2 のバイト順です。既定はリトルエンディアンです。

**ファイル名**は、Windows では OS に渡るところまで UTF-8 です。`src/jvim.manifest`
がプロセスのコードページを UTF-8 に指定しているためです。`🍣.txt` は開けますし、
タイトルバーにも正しく出て、`:` 行で補完もできます。これには Windows 10 1903 以降
が必要です。それより古い環境では従来どおりファイル名は CP932 です。

## 日本語入力

このエディタは挿入モードとコマンドモードの違いを知っていて、それに合わせて IME を
操作できます。それが `fepctrl` の要点で、今のエディタがだいたいやってくれないこと
です。

| | |
| --- | --- |
| `:set fepctrl` (`fc`) | 挿入・置換モードを抜けるときに IME を**切り**、入るときに戻します。ウィンドウごとの設定。既定は off。 |
| `:set fepkey=\\` (`fk`) | IME をトグルするコントロールキー。値は `@` から `_` の文字で、実際のキーはその文字と `0x1f` の and です。つまり `\\` は CTRL-\ (と CTRL-@)。Win32 GUI では `fepkey=[` にすると SHIFT+SPACE になります。 |
| `:set jinsertmode=a` (`ji`) | 挿入モードをどちらで始めるか。`a` ASCII、`j` 日本語、`A`/`J` は同じですが、もう一方の種類の文字を `jiauto` 文字ぶん入力すると自動で切り替わります。 |
| `:set jiauto=10` (`ja`) | 上の `A`/`J` の閾値。既定の 0 は自動切り替えなしです。 |
| `:set fepkeys=r` (`fo`) | **コマンドモード**のどのキーでも IME を開くか。テキストを入力するキー (`a A i I R cw` など) は指定に関わらず常に開きます。これは 1 文字やパターンを取るキー向けです。既定は `r`。`/?tTfF` を足すとよいでしょう。 |

**Windows では GUI を使ってください。** コンソールモードの日本語入力は不安定で
(RETURN を押すまで文字が表示されないことがあります)、2002 年当時もそうでした。
GUI では既知の問題はありません。

Unix で IME を制御するには、入力メソッドのプロトコルを話す `fepseq.c` を伴う
`FEPCTRL` を組み込む必要があり、`scripts/build-unix.sh` はこれを外しています。
なくても IME は端末の担当になるだけで、現代のデスクトップではそのほうが自然です。
どちらの経路でもエディタが受け取るのは確定後の UTF-8 です。プロトコルの詳細は
[doc.j/fepctrl.doc](doc.j/fepctrl.doc) にあります。

## 表示とフォント

**Win32 GUI は DPI 対応です。** 96 DPI で描いて引き伸ばすのではなく実際のピクセル
数でフォントを要求するので、125%・150% でも文字が鮮明です。フォントとウィンドウ
のサイズは設定時の DPI とともに保存され、表示される画面に合わせて読み替えられま
す。別倍率のモニタへウィンドウを移動したときも同じです。以前の「ぼやけるが大きい」
描画に戻したい場合は、exe のプロパティ > 互換性 > 高 DPI 設定の変更で上書きできます。

フォントダイアログの `linespace` / `charspace` は意図的に生のピクセル値です。1〜2
ピクセルの微調整のための値で、これを拡大するとダイアログの表示と実際が食い違います。

**文字幅。** 1 文字は 1 桁か 2 桁です。East Asian Ambiguous (`→`、`±`、ギリシャ
文字、キリル文字) は **2 桁**扱いにしています。日本語フォントが実際にそう描くから
で、後の Vim の `'ambiwidth'` を `double` にしたのと同じ意味です。U+2000 より下の
文字 (Latin-1、IPA、修飾文字) は 1 桁です。混植フォントはそれをラテン側から取る
ためです。結合文字は 0 桁です。

描画には既知の限界が 2 つあります。

- **GDI はカラー絵文字を描きません。** カラーのグリフレイヤーには DirectWrite が
  必要で、出てくるのはフォールバックフォントの白黒アウトラインです。幅と編集は
  正しく扱われます。
- **絵文字の異体字シーケンスは基底文字の幅になります。** `⚠️` は U+26A0 (Neutral
  なので 1 桁) と異体字セレクタ (0 桁) で、フォントは 2 桁で描くので隣にはみ出し
  ます。これを直すには、画面のセルが 1 コードポイントではなくシーケンスを持てる
  ようにする必要があります。

`crmark` (`cm`) は行末に印を付けます。文字は `crchar` (`cc`) で指定します
(漢字は指定できません、既定は `$`)。`list` でも同じことはできますが、タブまで
変換されてしまうのが不便な場面向けです。`trackset` (`trs`) は罫線に使う文字種で、
`as` が ASCII、`jp` が日本語の罫線です。

シンタックスカラーはそれ自体が小さな設定言語 (色の定義、`syntax link`、ファイル
種別ごとの正規表現ルール) になっています。`doc.j/readme.doc` §6.26 に全仕様があり、
このツリーでのルールの置き方は `syntax/README` にあります。

以前は Win32 GUI 専用でしたが、端末でも動くようになりました。同じ色を SGR
エスケープとして出します。その色をそのまま出せるかは端末次第で、判断には
`$COLORTERM` を使います。`truecolor` か `24bit` ならその色そのもの、それ以外なら
昔から端末にある 16 色のうち最も近いものになります。色を出せない端末では
`set nosyntax` してください。

1 点だけ、そこの記述は古くなっています。行をまたぐ領域 (C のコメントを色付けする
`p` 検索モード) は、以前は描画する行から前後 `synlines` 行を探して見つけていたため、
それより長いコメントや文字列は色が落ち、閉じていないものは最初から無色でした。この
ツリーでは代わりに、各行について「上の行が終わった時点でどの領域が開いていたか」を
覚えます。長さは関係なくなり、閉じていない領域はファイルの末尾まで色が付き、領域を
開く・閉じる記号を打った時点で下の行の色がすぐ変わります。§6.28 の `synlines` は
タグ検索 (`t` モード) にのみ効くようになりました。

どのファイル種別に色が付くかは、エディタ本体ではなくルールファイルの問題です。
2002 年の JVim にあったのは C/C++、Java、VBScript、HTML、`.bat`、`.ini`、`.def`、
`.rc`、そして `_vimrc` 自身でした。このツリーでは Python、JavaScript/TypeScript、
Go、Rust、Ruby、シェル、Markdown、JSON、YAML、TOML、SQL、CSS/SCSS、C#、PHP、Lua、
XML、diff、Makefile、Dockerfile を追加し、C のルールが `.cc` `.cxx` `.hpp` `.hxx`
`.hh` `.inl` にも効くようにしました。

ルールは種別ごとに 1 ファイルで `syntax/` にあります。配布パッケージでは exe の隣、
`make install` では `$VIM` に入ります。rc からは 1 行で全部に届きます。

```vim
set fexrc
set syntax
source $VIM/syntax/filetype.jvsyn
```

`filetype.jvsyn` がディスパッチャで、色とグループ名の `common.jvsyn` を読んでから、
開いたファイルに対応する 1 ファイルを読みます。種別を足すのは `syntax/zig.jvsyn` と
`filetype.jvsyn` の 3 行です。ルール言語で先に知っておかないと午後を無駄にする 2 点
を含め、詳細は `syntax/README` にあります。

`$VIM` がこれらを探す場所です。Windows ではエディタが exe のあるディレクトリを設定
するので、展開したパッケージなら何もいりません。Unix では `PREFIX` を変えていなければ
`/usr/local/lib/jvim3` です。ビルドツリーから直接動かす場合は自分で設定してください。

## vi に対して増えているもの

Vim 3.0 は vi にわずかな機能を足したもので、その「わずか」が使われた理由でした。

- **多段 undo。** `u` を何度でも。`CTRL-R` で redo。`undolevels` で段数。
- **複数ウィンドウ。** `:split`、`CTRL-W` 系のコマンド、`:buffers`
  ([doc/windows.doc](doc/windows.doc))。
- **コマンドライン履歴と補完。** `:` と `/` の履歴を矢印キーで辿れます。`wildchar`
  (TAB) でファイル名補完。
- **ブロック操作**、ほぼすべてのコマンドへの回数指定、`showmatch`、`smartindent`、
  ダイグラフ、記録、`:!` フィルタ、quickfix (`-e`、`:cn`)。
- **挿入モードでのキーワード補完** — `CTRL-N` / `CTRL-P`。

なくなっているのは `Q` コマンドだけです。両方向の違いをすべて書いたものが
[doc/difference.doc](doc/difference.doc) (日本語版は
[doc.j/differen.doc](doc.j/differen.doc)) です。

## JVim が足したもの

上に挙げた文字コードと IME のオプション以外に、日本語化で足されたものです。全文は
[doc.j/readme.doc](doc.j/readme.doc) §6〜§9 にあります。

| | |
| --- | --- |
| `jreplace` (`jrep`) | 置換モードを桁数ではなく文字数で数えます。`123456` の上から `R` で `あいう` と打つと `あいう456` になります (off なら `あいう`)。既定は on。 |
| `jtilde` (`jt`) | `~` を日本語にも効かせます (`あ` → `ア`、`♂` → `♀`)。既定は off。 |
| `jignorecase` (`jic`) | あいまい検索。`a`、`A`、`ａ`、`Ａ` が同じ文字として扱われます。既定は off。 |
| `jjoinspaces` (`jjs`) | `J` で日本語の行同士をつなぐときに空白を入れるか。 |
| `window` (`win`) | GUI のウィンドウサイズ。`:set win=80,25`。modeline からも使えます。 |
| `whichwrap` (`ww`) | 本来の 1〜16 のビットに加えて、32 と 64 で日本語・英数字・空白の境界における単語移動の止まり方が変わります。 |
| `option` | 「標準機能ではないが、あると便利」なもののビットマスク。`1` は `:` 行の `@` と `&` をこのファイル名とそのディレクトリにする、`32` は quickfix でエラー行以外を無視、`128` はオリジナルのバイナリモード。§6.19。 |
| `foption` | 同じくファイル単位で、読み書き時に適用されるもの。`1` 行末の空白を削除、`2` 全角空白を空白に、`4` タブを空白に、`8` 行頭の空白をタブに、`64` C ソースでの `%` ジャンプを賢く、`256` CR 改行 (旧 Mac) のファイルを読み書き。§6.20。 |
| `gh`, `gx`, `gp`, `gn`, `gg` | ヘルプ表示、罫線モード (ESC で終了、`gp`/`gn` で罫線の種類を変更)、grep ジャンプ (`grep -n` の出力をバッファに置いて、その行で `gg` するとタグジャンプのように飛びます)。 |
| `gX`, `gC`, `gV` | GUI: クリップボード経由の切り取り・コピー・貼り付け。Windows の CTRL-X / C / V と同じ動作。 |
| 拡張された正規表現 | Vim 5.7 相当の `\d \w \s \a \l \u \h \i \k \f \p \x \o` とその大文字版、`\e \t \r \b \n`。文字クラスは文字単位で比較するので、`[あ]` は `あ` にだけマッチします。§6.29。 |
| `map` 用のキー名 | `#[UP]`、`#[SLEFT]`、`#[F01]`〜`#[F20]`、`#[HELP]`、`#[UNDO]`、`#1`〜`#0`。§6.30。 |
| `CTRL-G` | 位置に加えて、カーソル位置の文字コードとファイルの文字コードも表示します: `line 36 of 36 --100%-- col 0/1 ch 0x31 [E]`。 |

## 削除した機能

JVim 2.1b にあった 3 つの機能を、再配布の条件が扱いにくかったためソースごと削除
しました ([README.ja.md](README.ja.md#ライセンス) 参照)。`doc.j/readme.doc` に
記述が見つかっても、それは歴史です。

| 削除したもの | 元の名前 |
| --- | --- |
| BDF ビットマップフォントの描画 | `USE_BDF` と `Global > BDF Font` メニュー。アウトラインフォントなら BDF より広い文字を持っています。 |
| LHA / ZIP / CAB / TAR 内のファイル編集と ftp 経由の編集 | `USE_EXFILE`。実行時に UNLHA32.DLL なども必要でした。 |
| MIME / uuencode / base64 のデコード — `gu` コマンドと `decode` オプション | `USE_MATOME`。同じディレクトリの一部です。**`set decode=a` の入った古い `_vimrc` は、起動時に unknown option を報告します。** |

`Global > Unicode Font` もなくなりました。ANSI と Unicode の描画 API を切り替える
項目でしたが、UTF-8 化以降、描画は無条件に Unicode です。

単体の `grep.exe`、`clip.exe`、`vim32s.exe` もビルドしていません (欲しい人のために
`src/makjnt.mak` には残してあります)。エディタ内の検索拡張 (`USE_GREP`、`gg`) は
入っています。

## 設定はどこに保存されるか

| | |
| --- | --- |
| Unix | `/usr/local/etc/jvim3rc`、次に `$VIMINIT` / `$HOME/.vimrc` / `$EXINIT` / `$HOME/.exrc` のうち最初の 1 つ、`exrc` が設定されていれば `./.vimrc` か `./.exrc`。 |
| Windows | `%VIM%\vimrc`、次に `%VIMINIT%` / `%HOME%\_vimrc` / `%EXINIT%` / `%HOME%\_exrc` のうち最初の 1 つ、`exrc` が設定されていればカレントディレクトリの `_vimrc` か `_exrc`。 |
| Windows の GUI 状態 | `HKEY_CURRENT_USER\Software\Vim`、および `-n0`〜`-n3` が選ぶ 4 プロファイル用の `\Software\Vim\0`〜`\3`。フォント、ウィンドウサイズ、色、メニューとスクロールバー。 |
| Windows の `vim32.ini` | exe の隣に置く ini ファイル。ウィンドウを作る前に判っている必要があるもの (行数、桁数、プリンタ、ビットマップ、サウンド) を読みます。`-I section` でセクションを選べます。`doc.j/readme.doc` §9.3。 |

古い JVim から移行する場合の注意が 1 つあります。古い版が書いた `vim32.ini` は
非 ASCII の値が CP932 になっていますが、Windows は BOM のない ini をプロセスの
コードページ (ここでは UTF-8) で読むため、文字化けします。JVim から設定を保存し
直せば直ります。レジストリのフォント名は自動で変換されます。

## トラブルシューティング

| | |
| --- | --- |
| **日本語が文字化けする** | `:set jm?` と `:set jc?`。`jc` はこのファイルの符号、`jm` の 2 文字目が端末に送っている符号です。Unix では `LANG` が両方を決めます (`LANG=ja_JP.UTF-8`)。 |
| **`:help` がファイルを見つけられない** | エディタが探すのは Windows なら `$VIM\vim.hlp`、Unix なら `/usr/local/lib/jvim3.hlp` です。`v3.0-j2.1b-utf8.4` までのパッケージは `jvim3.hlp` という名前なので、リネームするか `set helpfile=$VIM\jvim3.hlp`。 |
| **新規ファイルが意図しない符号で保存される** | `jmask` の 4 文字目です。`:set jm=SSST` ならパイプは CP932 のまま新規ファイルは UTF-8 になります。 |
| **拡大表示で文字がぼやける・不均一** | 本来は起きないはずなので報告してください。回避策は exe のプロパティ > 互換性 > 高 DPI 設定の変更です。 |
| **フォントダイアログに使いたいフォントが出ない** | 等幅フォントだけを出す仕様です。文字グリッド上ではプロポーショナルフォントは描画できません。 |
| **起動時に unknown option と出る** | 古い JVim の rc ファイルです。多いのは `decode` で、[削除した機能](#削除した機能)を参照してください。`v3.0-j2.1b-utf8.5` 以降で同梱する `_jvimrc.sample` ではもう出ません。 |
| **日本語入力で文字が落ちる・遅れて出る** | Windows のコンソールモードです。`jvim32w.exe` を使ってください。 |
| **カーソルキーで文字が入る・画面が乱れる** | Unix の `$TERM` です。ビルド時に curses/termcap ライブラリが見つかればそれを使い、なければ組み込みの端末定義 (対応端末は少なめ) を使います。どちらかは `./scripts/build-unix.sh` が表示します。 |
| **クラッシュした** | Windows では `%LOCALAPPDATA%\jvim3\` にレポートが書かれます。`scripts/resolve-crash.sh <report.log>` に対応する `.debug` ファイルを添えると、アドレスを関数名と行番号に変換できます。issue に貼ってください。 |
| **その他** | <https://github.com/kuwa72/jvim3/issues> へ。何を打って何が起きたか、どのビルドか、文字が絡むなら `:set jm? jc?` の結果を添えてください。 |
